{
  "name": "@vitacircle/ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000 --app-dir .",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest -q",
    "lint": "echo lint-ai"
  }
}
